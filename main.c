//load file to array and sort then
#include <stdio.h>
#include <stdio.h>


int main(){
/*
int array_length=1;
int *array;
int *newarray;

newarray = (int *)
    realloc(array, (array_length + 1) * sizeof(int));
if (!newarray)
    error("out of memory");
array = newarray;
array[array_length++] = x;*/
	int i;
	do{
	 scanf("%d\n",&i);
	printf("%d\n",i);
	}while(i != NULL);

	return 0;
}